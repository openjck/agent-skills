{
  "name": "agent-skills",
  "private": true,
  "type": "module",
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@openjck/prettier-config": "^1.0.2",
    "prettier": "^3.8.4"
  }
}
